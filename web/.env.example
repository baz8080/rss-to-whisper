APP_DB_PATH=/path/to/podcasts.db
APP_AUDIO_BASE_URL=http://your-nas:9280
# Optional: the pipeline's data directory. Set it to enable word-level
# highlighting on the episode page, which reads words.jsonl.gz from beside each
# episode's audio. Leave it out and the feature stays hidden.
#APP_DATA_DIRECTORY=/path/to/data_directory
