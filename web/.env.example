APP_DB_PATH=/path/to/podcasts.db
# The pipeline's data directory, served over HTTP. Audio and words.jsonl.gz are read from
# under it; word-level highlighting stays hidden unless this is an absolute URL.
APP_DATA_URL=http://your-nas:9280
